# Core dependencies
flask==2.3.3
flask-cors==4.0.0
python-dotenv==1.0.0
gunicorn==21.2.0

# Google AI dependencies
google-generativeai==0.3.2

# Machine Learning and NLP
transformers==4.35.2
torch==2.1.1
scikit-learn==1.3.2
pandas==2.1.3
numpy==1.24.4

# Built-in modules (không cần cài đặt):
# sqlite3, os, json, time, traceback, datetime, re, pickle, logging

# Optional: For better performance
# accelerate==0.24.1  # For faster transformers inference